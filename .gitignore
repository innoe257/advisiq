# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*

# Env / secrets
.env
.env.*
!.env.example

# ML artifacts — small ones (like the current synthetic demo model) are
# committed directly; exclude anything large by ignoring the directory and
# allow-listing specific files.
backend/app/ml/artifacts/*
!backend/app/ml/artifacts/.gitkeep
!backend/app/ml/artifacts/risk_model_v1.json

# Node / frontend (Phase 2)
frontend/node_modules/
frontend/.next/
frontend/out/

# OS / editor
.DS_Store
.vscode/
.idea/
.claude/settings.local.json

# Alembic
backend/alembic/versions/__pycache__/
